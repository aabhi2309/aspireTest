export interface SideTabsType {
    icon:string,
    name:string
} 

export interface DebitCardType {
    cardNumber: string,
    validThru:string,
    name:string
}

export interface FeaturesType {
    id:string,
    name:string
}